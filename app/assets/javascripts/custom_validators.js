// The methods added via addClassRules can be accessed by using the function name as class name for the element
// The methods added via addMethod can be accessed as a rule
// and then using $('form').validate();